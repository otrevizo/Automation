# docker/

Secrets-free `docker-compose.yml` and an `.env.example` template for the n8n setup go here. Real secrets stay in a password manager and a local, git-ignored env file — never committed.

Currently empty — to be added once the Compose file is cleaned of anything environment-specific/secret.
