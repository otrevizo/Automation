# workflows/

Exported n8n workflow JSON goes here, one file per use case (e.g. `finance_stock_forecast.json`). Export with credentials/secrets stripped — n8n workflow exports can include credential references, so double-check before committing.

Currently empty — first workflow (finance/stock forecast use case) still in progress.
